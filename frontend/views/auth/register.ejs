<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Register - <%= type === 'admin' ? 'Admin Portal' : 'User Portal' %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white h-screen flex items-center justify-center">

    <div class="w-full max-w-md bg-gray-800 p-8 rounded shadow border-t-4 <%= type === 'admin' ? 'border-red-600' : 'border-blue-600' %>">
        
        <h2 class="text-3xl font-bold text-center mb-6">
            <%= type === 'admin' ? '🛡️ Admin Registration' : '👤 User Registration' %>
        </h2>

        <form action="/api/auth/register" method="POST" class="space-y-4">
            
            <input type="hidden" name="role" value="<%= type %>">

            <div>
                <label class="block mb-1 font-bold">Full Name</label>
                <input type="text" name="name" required class="w-full p-2 rounded bg-gray-700 border border-gray-600 focus:border-white">
            </div>

            <div>
                <label class="block mb-1 font-bold">Email Address</label>
                <input type="email" name="email" required class="w-full p-2 rounded bg-gray-700 border border-gray-600 focus:border-white">
            </div>

            <div>
                <label class="block mb-1 font-bold">Password</label>
                <input type="password" name="password" required class="w-full p-2 rounded bg-gray-700 border border-gray-600 focus:border-white">
            </div>

            <% if(type === 'admin') { %>
                <div>
                    <label class="block mb-1 font-bold text-red-400">Admin Secret Key</label>
                    <input type="password" name="adminSecret" placeholder="Required for Admin Access" class="w-full p-2 rounded bg-gray-700 border border-red-500">
                    <p class="text-xs text-gray-500 mt-1">* Ask the system owner for this key.</p>
                </div>
            <% } %>

            <button type="submit" class="w-full py-3 rounded font-bold mt-4 <%= type === 'admin' ? 'bg-red-600 hover:bg-red-700' : 'bg-blue-600 hover:bg-blue-700' %>">
                Register as <%= type === 'admin' ? 'Admin' : 'User' %>
            </button>
        </form>

        <div class="mt-4 text-center">
            <a href="/login?type=<%= type %>" class="text-gray-400 hover:text-white">Already have an account? Login</a>
            <br>
            <a href="/select-role" class="text-sm text-gray-500 mt-2 inline-block hover:underline">← Change Portal</a>
        </div>
    </div>

</body>
</html>